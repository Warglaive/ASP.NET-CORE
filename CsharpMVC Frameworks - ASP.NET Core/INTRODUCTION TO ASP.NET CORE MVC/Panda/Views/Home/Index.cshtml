@using Microsoft.AspNetCore.Identity
@model Panda.ViewModels.UserViewModel
@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager
<main class="mt-3">
    @if (!this.SignInManager.IsSignedIn(User))
    {
        <div class="jumbotron mt-3 bg-panda">
            <h1 class="text-white">Welcome to PANDA Delivery Services.</h1>
            <hr class="bg-white hr-2" />
            <h3 class="text-white"><a href="/users/login">Login</a> if you have an account.</h3>
            <h3 class="text-white"><a href="/users/register">Register</a> if you don't.</h3>
        </div>
    }
    else
    {
        <div class="text-center text-torshia">
            <h1 class="text-center text-torshia">Hello, @UserManager.GetUserName(User)!</h1>
            <hr class="bg-panda w-100 hr-2" />
        </div>
        <div class="row">
            <div class="col-sm-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title d-inline-block">Special title treatment</h5>
                        <a href="#" class="btn btn-primary float-right">Go somewhere</a>
                    </div>
                </div>
            </div>
            <div class="col-sm-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title d-inline-block">Special title treatment</h5>
                        <a href="#" class="btn btn-primary float-right">Go somewhere</a>
                    </div>
                    <div class="card-body">
                        <h5 class="card-title d-inline-block">Special title treatment</h5>
                        <a href="#" class="btn btn-primary float-right">Go somewhere</a>
                    </div>
                </div>
            </div>
            <div class="col-sm-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title d-inline-block">Special title treatment</h5>
                        <a href="#" class="btn btn-primary float-right">Go somewhere</a>
                    </div>
                </div>
            </div>
        </div>
    }
</main>